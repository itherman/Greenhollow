rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // This project doesn't currently use Cloud Storage; keep it locked down.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}


